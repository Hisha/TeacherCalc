<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layoutMain"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Main" >

    <Button
        android:id="@+id/btnGetAnswer"
        android:layout_width="200dip"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/btnGetAnswer" />

    <EditText
        android:id="@+id/txtQuestionCount"
        android:layout_width="@dimen/txtQuestionCount"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnGetAnswer"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/txtQuestionCount_margin"
        android:inputType="number" >
    </EditText>

    <TextView
        android:id="@+id/lblQuestionCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/txtQuestionCount"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/lblQuestionCount_margin"
        android:text="@string/lblQuestionCount"
        android:textStyle="bold" />

</RelativeLayout>